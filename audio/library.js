/* audio/library.js —— 歌库清单（这是数据，不是逻辑）
 *
 * 这个文件由 tools/scan-audio.js 自动生成，不用手改：
 * 把音频丢进 audio/ 目录，然后在项目根目录跑 `node tools/scan-audio.js` 重新扫一遍。
 * 文件名写成「歌手 - 歌名.mp3」会自动拆分歌手和歌名，文件里有标签就优先用标签。
 *
 * 首页的音乐卡和「歌库」页读的都是这份清单。
 *
 * ── 为什么这份清单是空的 ──────────────────────────────────────────
 * 本站发布在公开仓库（GitHub Pages），因此不收录商业歌曲的完整音频：
 * 把商业歌曲传到公开站点属于侵权风险，而且体积过大（本机 25 首约 237MB）。
 * 所以 audio/ 里的音频文件一律不进 git（见根目录 .gitignore），清单保持为空。
 *
 * 页面对空清单的处理：
 *   - 「歌库」页显示「歌库还空着。」；
 *   - 首页音乐卡不加载任何曲目，右上角问「想不想来点音乐？」时不会出声；
 *   - 把本地音频拖进音乐卡仍可自己试听（只在本机有效，不会上传）。
 *
 * 想在本机恢复自己的歌单：
 *   1) 跑 `node tools/scan-audio.js` 重新生成这份清单（本地听得到，但不会提交）；
 *   2) 提交前还原成本文件：git checkout -- audio/library.js
 *
 * 想上线音乐又不侵权：换成本人演奏/录制，或明确可商用、可再分发（如 CC0 / 公有领域）
 * 的音频，再跑一次扫描脚本。CREDITS.md 的「音频」一节有同样的说明。
 *
 * 一首歌一行：{ title: "曲名", artist: "歌手", src: "audio/文件名.mp3", dur: "3:42" },
 * 注意：除了这里的注释，别在花括号外写坏语法，否则整页音乐模块会起不来。
 */
window.AUDIO_LIBRARY = [];
